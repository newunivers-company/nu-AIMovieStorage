# torch 는 PyTorch 의 cu128 인덱스에서. 판은 업스케일 엔진과 같게 맞춰 두었습니다 —
# 같은 GPU 에 다른 판이 두 벌 깔리면 드라이버 문제를 가려내기가 어렵습니다.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
# ACE-Step 은 **PyPI 에 없습니다.** 2026-09-16 설치 실패: 「acestep was not found in the package registry」 —
# `pip install acestep` 로 받는 것인 줄 알고 판만 적어 두었는데, 저장소 이름은 `ace_step` 이고 배포는 깃허브뿐입니다
# (setup.py 의 name=ace_step, version 0.2.0). 그래서 **커밋을 못 박아** 깃에서 받습니다 — 판이 흐르면 우리 워커와 어긋납니다.
#
# 딸려 오는 것(datasets·pytorch_lightning·transformers==4.50.0 …)은 저장소의 requirements.txt 가 정합니다.
# torch 는 위에서 우리 판으로 먼저 못 박아 두어 그대로 남습니다(--index-strategy unsafe-best-match).
ace_step @ git+https://github.com/ace-step/ACE-Step@1bee4c9f5b43e30995f8d4d33b3919197ce1bd68
# torchaudio 2.x 는 소리를 **저장할 때** torchcodec 을 부릅니다. 없으면 음악을 다 만들어
# 놓고 마지막에 「TorchCodec is required for save_with_torchcodec」 로 죽습니다
# (2026-09-18 실측: 39초에 올라가 6초 만에 이 오류).
torchcodec==0.16.0
soundfile==0.13.1
librosa==0.11.0
