/**
 * 화면 폭을 여기 한 곳에서 정합니다.
 *
 * # 왜 모아 두는가
 *
 * 페이지마다 `max-w-4xl`·`max-w-5xl`·`max-w-6xl` 이 흩어져 있었습니다.
 * 그래서 «좀 넓히자» 를 할 때마다 네 파일을 따로 고쳐야 했고, 한 곳을
 * 빠뜨리면 화면마다 폭이 달라 보였습니다.
 *
 * # 왜 넓혀야 했는가
 *
 * 이 앱의 작업 화면은 **인물·장소·에셋을 한눈에 늘어놓는 자리**입니다.
 * 1024px 로 묶어 두니 2560px 모니터에서 가운데 40%만 쓰고 양옆이 텅 비었고,
 * 계보 패널은 카드 한 장 폭도 안 나와 부모와 변형을 함께 볼 수가 없었습니다.
 */

/**
 * 작업 화면. 자원을 늘어놓는 곳이라 넓게 씁니다.
 *
 * 1800px 에서 멈추는 이유는 그보다 넓어지면 왼쪽 끝과 오른쪽 끝을 한 번에
 * 훑을 수 없기 때문입니다. 큰 모니터에서는 눈이 아니라 목이 움직입니다.
 */
export const WORK_WIDTH = "mx-auto w-full max-w-[1800px] px-6";

/**
 * 글을 읽는 화면. 설정처럼 문장이 많은 곳입니다.
 * 글줄이 너무 길면 다음 줄 첫 글자를 놓칩니다.
 */
export const READING_WIDTH = "mx-auto w-full max-w-6xl px-6";

/**
 * 인물·장소·에셋·변형 편집 창. **본문(WORK_WIDTH)과 같은 1800px** 까지 넓힙니다.
 *
 * 창이 본문보다 좁아서(1024px) 한글·영문 프롬프트 네 칸이 세로로 길게 늘어져
 * 한눈에 안 들어왔습니다. 창과 본문의 너비가 다르면 같은 프롬프트가 두 곳에서 다르게
 * 접혀, 밖에 옮겨 붙일 글을 창 안에서 가늠할 수 없습니다.
 */
export const EDITOR_DIALOG =
  "max-h-[calc(100vh-2rem)] w-[calc(100vw-2rem)] max-w-[1800px] overflow-y-auto border-0 p-0 text-white sm:max-w-[1800px]";
